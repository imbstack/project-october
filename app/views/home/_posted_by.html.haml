.posted_by
  - if article.poster.is_a?(Feed)
    from
    = article.poster.name
  - elsif article.poster.is_a?(User)
    by
    = link_to article.poster.name, user_path(article.poster)
  &middot; posted
  = distance_of_time_in_words(article.created_at, Time.now)
  ago
